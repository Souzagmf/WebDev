const optionImgae = document.querySelectorAll(".option-image")

function HandleOptionClick(event){
    const clickImage = (event.currentTarget)
}

optionImgae.forEach(img =>{
    img.addEventListener("click", HandleOptionClick)
})

